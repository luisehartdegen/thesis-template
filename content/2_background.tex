\chapter{Background}\label{ch:background}

A short explanation of both softwares involved will provide context for the following examination of the current workflow and how it can be improved in more detail. 
The first software, Autodesk Fusion, is used for ``design, engineering, electronics, and manufacturing [...] all-in-one''\cite{autodesk_inc_overview}. 
It provides functionalities for the ``entire product development process [...] with integrated 3D CAD, CAM, CAE, and PCB''\cite{autodesk_inc_overview}, meaning it can be used for designing parts to manufacture in either additive or subtractive processes. 
Mainly relevant to the process at hand is the CAD (Computer Aided Design) functionality. 
Contrary to 3D modelling software like Blender intended for purely digital products, CAD is based on precise measurements, taking into account not only the surface but also the body's volume. 
It provides visualization tools for examining and analyzing designs, as well as simulation capabilities, for example, to assess how different components interact or respond to various mechanical stresses.

Slicer software on the other hand imports a 3D model and ``slices [it] into layers for the printer [...] allow[ing] users to select print settings, such as layer height and print speed''\cite{hudson_understanding_2016}
It therefore translates the model into step-by-step commands for a specific printer.
There are many different slicers available online nowadays, either general ones or ones specific to printer manufacturers. Since ``getting all the settings right for a particular 3D printer can be fiddly, a lot of manufacturers modify one of these open source programs and create a proprietary program just for their machine.''\cite[p.~58]{horvath_mastering_2018}
\textit{OrcaSlicer} is one the open source slicers, originally forked from \textit{Bambu Studio}, which itself was forked from \textit{PrusaSlicer}, a descendant of \textit{Slic3r}. 
``Slic3r is licensed under the GNU Affero General Public License, version 3''\cite{softfever_orcaslicerdoc_nodate}, which means that every software using parts of it has to be licensed under the same license.
Since these slicers are all intricately linked, oftentimes new features are being introduced in one of them and then pulled over into the others as well.

OrcaSlicer always handles files as projects, either by interpreting 3MF files as a project in itself or by opening a file within a new project. 
That way the user has the option to save project specific settings or presets for further use or just export the generated g-code for printing and discard said settings. 

\section{State of the Art Workflow from Fusion to OrcaSlicer}

\begin{wrapfigure}[11]{r}{0.48\linewidth}
    \vspace{-10pt}
    \includegraphics[width=\linewidth]{3dmodel_box.png}
    \caption{3D model of simple box}
    \label{simple-3d-model-box}
\end{wrapfigure}

When analyzing the process from idea to print, several main steps can be identified as shown in \ref{workflow}. 
``[Y]ou need to create or acquire a 3D computer-aided design (CAD) model, slice this model into layers and generate the commands needed to physically create the model, and, finally, physically create the print.''\cite[p.~52]{horvath_mastering_2018}
Using a simple box with a snap-fit closure and two different filament colors as an example, the following analysis will focus on the steps from exporting a model to printing, highlighting in particular how the workflow differs depending on the file format used in regards to three points.

\begin{enumerate}
    \item A model consisting of multiple parts, some of which are not positioned on the surface they need to be printed on (e.g. the lid)
    \item Using multiple materials (e.g. printing the text in a different color)
    \item Using model parts as modifiers (e.g. the translucent green boxes as modifiers to print more wall loops in the closure's area for higher stability)
\end{enumerate} 

Exporting the model from Fusion is possible via two dialogs: the general export and the 3D print specific one.
The two dialogs each offer a different set of file formats due to their different purposes.
Since this analysis deals with 3D printing in particular, only the export dialog specific to 3D printing will be considered, which offers to export as STL, 3MF and OBJ format. 
For all three the user can decide whether to generate one file containing all selected components and bodies, or one file per body. 

\subsection{Exporting as STL}

Exporting the model as an STL file immediately shows one important distinction. 
``STL native files describe only the surface geometry of a three dimensional object without any representation of color, texture or other common CAD model attributes,''\cite{iancu_about_2018} meaning that even if a user applies materials in Fusion, that information won't be exported, and the user has to apply all materials anew in OrcaSlicer. 

Furthermore there will always be one body in the resulting file, even if the model is comprised of multiple bodies within Fusion. 
Therefore the user has to choose whether to combine all bodies into one or exporting each body as a separate file.
The first option results in one extra step to split the model into separate objects within OrcaSlicer to be able to print the text in a different color or to use the green boxes as modifiers. 
Alternatively exporting each body as a separate file might loose their spatial relations to each other while keeping the bodies separate.
OrcaSlicer does however ask the user whether the files should be loaded as a single object with multiple parts. 
The advantage over loading each file as an object is the preservation of the spatial relation and thus parts, like the modifiers in the example box, can be placed at any height while a single object is always placed with at least one surface flush with the printing plate. 

\subsection{Exporting as OBJ}

The OBJ file format ``is the preferred 3D printer file format for multicolor printing''.\cite{iancu_about_2018}
Consequently, OrcaSlicer shows a particular dialog (fig. \ref{fig:obj_color_dialog}) when opening an OBJ file, asking the user how they want to map the file's colors to current or new filament presets.
This makes it easy for users to align filament colors (i.e. the color the filament is represented as in OrcaSlicer, not necessarily the color of the filament itself) to colors applied in Fusion.
Just like STL files though, the model is generated as one body per file resulting in the same dialogs and extra steps.

\subsection{Exporting as 3MF}

Unlike the other two formats, 3MF not only preserves separate bodies even when exported as a single file making it more user-friendly, it also includes ``information about the model’s color, material, and textures.''\cite{iancu_about_2018}
Initially when importing a 3mf file containing multiple objects, OrcaSlicer asks the user whether the file should be loaded as a single object with multiple parts instead of considering them as multiple objects similar to the dialog when importing multiple STL or OBJ files at once.

While Fusion does export materials as RGBA colors in one hexcode per body, OrcaSlicer does not offer the user to map these to available or new presets so far. 
Consequently users have to reapply materials manually which has been criticized by users, e.g. in \textit{OrcaSlicer issue \#2438}\footnote{Single Object Multiple parts with Multiple Colours: [...] I have made a number of 3D models with multiple parts in single 3mf file. I mostly use Fusion 360 for models and during modeling I assign colours to each part. But when I upload this 3mf file to the slicer, It assigns the primary filament colour to all the parts. I was wondering if the slicer would be able to distinguish the parts with different colours and assign either the original colour to the parts or even randon colours(which can be changed later).
For example if my 3mf file has 5 colour parts, the slicer should be to detect them. Then it should find which colour has the maximum spread by area and assign the filament 1 to it. The second largest should be assigned the second filament and so on till the last.[...] \url{https://github.com/SoftFever/OrcaSlicer/discussions/2438}}

\subsection{Applying Materials}

Generally, materials can be set using system or user defined presets per object. 
Material settings entail, amongst others, the filament itself, nozzle temperature, general printing speed and cooling settings.
OrcaSlicer actively asks the user, whether they want to save any changes to material parameters they have made, which makes it easy to save either parameters specific to one's printer and filament or even specific to one project while simultaneously keeping default material presets untouched.

\subsection{Applying Settings with and without Presets}

Settings like the amount of wall loops, infill percentage and pattern, and printing speed for single layers might need to be customized. 
It is possible to save these as presets as well, however only globally, \ie they can only be applied to the entire project, not to specific objects within.
As criticized in the \textit{OrcaSlicer Issue \#7769}\footnote{Copy/paste per-object settings: When planning/building large projects in-slicer it often becomes an extremely tedious task to click all the objects you want a specific set of settings to apply to. The larger the assembly/project you are planning, the easier it is to miss a body and make a mistake. If you "assemble" models together you cannot edit multiple sub-objects across assemblies.\\ \url{https://github.com/SoftFever/OrcaSlicer/issues/7769}}, particularly for projects containing multiple objects, using presets in this case is not a valid option. 

\section{The 3MF File Format}

While each of the three file formats mentioned have their own advantages and disadvantages, preliminary analysis indicated that the 3MF format represents the most suitable solution for the problem at hand.
3MF has been developed by the 3MF Consortium, founded by Microsoft, Autodesk, HP, 3D Systems, Prusa Research and several other key players in the field, to create a 3D printing file format that addresses the shortcomings of previous standards (STL, OBJ, AMF).
In particular the possibility for extensions and the support of colors as well as several different 3D model representations were a main factor, making it ideal for state-of-the-art additive manufacturing.\cite{rossel_security_2023}

\input{figures/3mf_file_structure.tex}

``A 3MF file is a ZIP archive that contains a complex file structure, defining a 3D model in Extensible Markup Language (XML) file''\cite{rossel_security_2023} usually called \texttt{3dmodel.model}, as seen in \ref{3mf_file_structure}. Optionally, there can be multiple 3D model files, one of which needs to be identified in the StartPart relationship as the root model.
The 3D model part is comprised of two sections per specification: ``a set of resource definitions that include objects and materials, as well as a set of specific items to actually build''.\cite{3mf_consortium_3mf_2025}

Taking a closer look at the composition of said XML file as it is exported from Fusion, see \ref{lst:3dmodel.model}, these sections can be found in lines 4 ff. (the \texttt{resources} tag), enclosing the \texttt{m:colorgroup} and \texttt{object} tags, and lines 41 ff. containing the \texttt{build} tag.
While materials can be represented in a number of different resource tags (e.g. basematerials, m:texture2d, m:compositematerials\dots), Fusion seems to solely use the \texttt{m:colorgroup} tag. 
Unfortunately there aren't a lot of information available as to the exact way Fusion exports 3MF, hence this is based on explorative observations.
The \texttt{colorgroup} contains two or more colors as an RGBA hexcode each, as mentioned before.
Per the additional Materials and Properties Extension specification\cite{3mf_consortium_materials_2025}, colors are specifically expressed in sRGB colorspace in accordance with the World Wide Web Consortium. 
Each group is identifiable by its \texttt{id} which is referenced in each object as \texttt{pid} while the colors themselves implicitly get a 0-based index.
Despite the colors' representation as an RGBA hexcode, thus including an 8-bit channel for opacity, the standard specifies that color properties \textit{should not} reference translucent display properties. 
If required, a multi-properties group must be used instead. 
Empirical observations indicate compliance with this requirement, as the opacity value is consistently set to \texttt{FF}, \ie a fully opaque color. 

The following \texttt{object} tags each contain attributes (body id, type, color id) as well as one triangular geometry mesh representation of an object volume. 
The latter is represented by vertices (x-, y-, z-coordinates in the unit specified within the model's attributes) and triangles referencing three vertices each oriented with normals pointing towards the exterior of their object.\cite{3mf_consortium_3mf_2025}
Meshes of different objects may intersect (as seen in the above mentioned example's case of wall loop modifiers) or self-intersect, in which case it must be specified which areas are inside or respectively outside the volume. 

A so-called PrintTicket can be attached to each 3D model part detailing user intent and device configuration information for printers. This might be useful for the intended purpose of print hints but has not been further pursued as an idea due to the way, the file is being handled in \textit{OrcaSlicer}.

In regards to adding print hints directly to the model's xml structure, it shall be noted that in accordance with the specification ``XML content MUST NOT contain elements or attributes drawn from the "xml" or "xsi" namespaces unless they are explicitly defined in the XSD schema or by other means in the specification.''\cite{3mf_consortium_3mf_2025}
