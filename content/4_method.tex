\chapter{Method}\label{ch:method}

what’s the solution(s) I came up with $\rightarrow$ sketching out different possibilities and why I chose the one I did 

what exactly are print hints 

The task of setting printing parameters correctly is by nature a complex one, due to the many different printers, materials and needs out there. 
Therefore simplifying the menu itself would be counterproductive, robbing more advanced users of options or making tasks even more frustrating by hiding menus.
Accordingly another possibility to reduce the task's complexity lies in ``predicting'' the user's next steps.

\section{Writing print hints to 3MF file in Fusion}
\begin{itemize}
    \item possible via add-in?
    \item only for marking surfaces / specific triangles as ``down'', i.e. the surface to be flush with the printing bed $\rightarrow$ modifiers for now (as a proof of concept) marked via specific materials
    \item can these be saved to Fusion's proprietary project files?
    \item can I just add them with a custom 3mf export function or can they be written to the 3mf file using Fusion's default export? 
    \item no need to 
\end{itemize}

\section{Analysis of OrcaSlicer's 3MF file Processing}
\begin{itemize}
    \item it does read in all the xml lines from the \textit{3dmodel.model} file including the materials but it doesn't write them to the model 
    \item add model diagram here 
\end{itemize}

\section{Implementation Design}
different possible approaches to solve the problem at hand and why did I do it the chosen way? 

As mentioned before, as a proof of concept using specific materials to represent the possible modifiers was the way to go. 
The idea was to save user presets locally containing a map of ``materials'', i.e. the RGBA hexcodes, to chosen modifier presets or actual filaments respectively. 
When opening a new 3MF project, the user would be prompted to either confirm the material to modifier / filament mappings or select options for newly detected materials. 
This reduces the need to apply settings upon each import while simultaneously giving the user the option to easily change mappings. 

\begin{itemize}
    \item software architecture (diagram?)
    \item input, output
    \item choices that had to be made 
\end{itemize}